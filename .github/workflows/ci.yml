# Pinned action SHAs (resolved 2026-05-23 for supply-chain security):
#   golangci/golangci-lint-action@v8.0.0
#     tag object SHA : 4afd733a84b1f43292c63897423277bb7f4313a9
#   v8 + golangci-lint v2 are required: v1.x was built with Go 1.23 and cannot
#   load configs targeting Go 1.24 (this project's go.mod minimum).
#   GitHub Actions `uses:` pins the tag object SHA for annotated tags.
#   To update: re-resolve via `gh api repos/golangci/golangci-lint-action/git/refs/tags/<ver>`
#   and update both this comment and the lint step in backend-test.
name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  backend-test:
    name: Backend tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Check for Go source files
        id: gocheck
        run: |
          if find . -name '*.go' -not -path './vendor/*' -not -path './node_modules/*' -print -quit | grep -q .; then
            echo "has_go=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_go=false" >> "$GITHUB_OUTPUT"
            echo "::notice::No Go source files yet — skipping vet and test."
          fi
      - name: go vet
        if: steps.gocheck.outputs.has_go == 'true'
        # frontend/dist/.gitkeep is committed so the embed.go directive
        # compiles without requiring the full SPA bundle. Runtime SPA mount
        # is gated on the presence of index.html (see internal/server/spa.go).
        run: go vet ./...
      - name: go test
        if: steps.gocheck.outputs.has_go == 'true'
        run: go test ./... -race -count=1
      - name: make generate
        if: steps.gocheck.outputs.has_go == 'true'
        # Runs protobuf codegen (if .proto files exist) or prints a notice and exits 0.
        # Requirements: MVP-FND-1.13. Design: §21.
        run: make generate
      - name: golangci-lint
        if: steps.gocheck.outputs.has_go == 'true'
        # Pinned by tag object SHA — see comment at the top of this file.
        # golangci/golangci-lint-action@v8.0.0 (v2 schema, supports Go 1.24+)
        # SHA: 4afd733a84b1f43292c63897423277bb7f4313a9
        # Requirements: MVP-FND-1.11, MVP-FND-9.9. Design: §14, decision #23.
        uses: golangci/golangci-lint-action@4afd733a84b1f43292c63897423277bb7f4313a9
        with:
          version: v2.12.2
          args: --config=.golangci.yml

  frontend-build:
    name: Frontend build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for frontend
        id: fecheck
        # Emit has_frontend=true only when frontend/package.json exists.
        # Requirements: MVP-FND-1.12, MVP-FND-9.5. Design: §14, decision #24.
        run: |
          if [ -f frontend/package.json ]; then
            echo "has_frontend=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_frontend=false" >> "$GITHUB_OUTPUT"
            echo "::notice::No frontend/package.json — skipping frontend build."
          fi
      - uses: actions/setup-node@v4
        if: steps.fecheck.outputs.has_frontend == 'true'
        with:
          node-version-file: 'frontend/.nvmrc'
      - name: npm ci
        if: steps.fecheck.outputs.has_frontend == 'true'
        working-directory: frontend
        run: npm ci
      - name: lint (tsc --noEmit)
        if: steps.fecheck.outputs.has_frontend == 'true'
        working-directory: frontend
        run: npm run lint
      - name: build
        if: steps.fecheck.outputs.has_frontend == 'true'
        working-directory: frontend
        # spec MVP-FND-9.5 mandates `npm ci && npm run build`
        run: npm run build
      - name: gzipped bundle size check
        if: steps.fecheck.outputs.has_frontend == 'true'
        working-directory: frontend
        # NFR-BUNDLE: warn when total gzipped JS exceeds 500 KB. This is a
        # soft check — we annotate the run but do not fail it so a single
        # bloated dep doesn't block release. Tune the threshold if needed.
        run: |
          if [ ! -d dist/assets ]; then
            echo "::warning::frontend/dist/assets/ missing — skipping size check"
            exit 0
          fi
          total=0
          for f in dist/assets/*.js; do
            [ -f "$f" ] || continue
            sz=$(gzip -c -- "$f" | wc -c)
            total=$((total + sz))
          done
          echo "Gzipped JS total: $total bytes ($(awk "BEGIN { printf \"%.1f\", $total/1024 }") KB)"
          if [ "$total" -gt 512000 ]; then
            echo "::warning::Gzipped JS bundle ($total bytes) exceeds 500 KB target"
          fi
      - name: upload frontend/dist
        if: steps.fecheck.outputs.has_frontend == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/
          retention-days: 7
          if-no-files-found: error

  backend-build:
    name: Backend build (${{ matrix.goos }}/${{ matrix.goarch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            goos: linux
            goarch: amd64
          - runner: ubuntu-latest
            goos: linux
            goarch: arm64
          - runner: macos-latest
            goos: darwin
            goarch: arm64
          - runner: windows-latest
            goos: windows
            goarch: amd64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: go build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        # frontend/dist/.gitkeep is committed so go build succeeds without
        # running the frontend pipeline first. For a production binary with
        # the embedded SPA, run `make build-with-ui` (or use a release flow
        # that downloads the frontend-build artifact).
        run: go build ./...
